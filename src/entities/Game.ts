export default interface Game {
  id: string;
  title: string;
  totalRating: number;
  platform: string;
  rating: string;
  logo: string;
  background: string;
  count: number;
  price: number;
  description: string;
}
